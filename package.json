{
  "name": "dcard-reader",
  "private": true,
  "scripts": {
    "lint": "lerna run lint --stream",
    "start": "lerna run start --stream"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lerna": "^4.0.0"
  }
}
